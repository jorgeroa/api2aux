# MCP Worker URL (required for the Hosted tab in MCP Tools dialog)
# Set this to your deployed MCP worker URL to enable hosted MCP deployment.
# When running locally with the combined server, use:
VITE_MCP_WORKER_URL=http://localhost:8787
